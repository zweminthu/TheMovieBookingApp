<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheMovieBookingApp"
        tools:targetApi="31">
        <activity
            android:name=".activities.VerifyPhoneNoActivity"
            android:exported="false">

        </activity>

        <activity
            android:name=".activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".activities.GetOTPActivity"
            android:exported="false">

        </activity>

        <activity android:name=".activities.LocationActivity"
            android:exported="false">

        </activity>

        <activity android:name=".activities.MainActivity"
            android:exported="false">


        </activity>

        <activity
            android:name=".activities.MovieDetailsActivity"
            android:exported="false">

        </activity>

        <activity android:name=".activities.SelectCinemaActivity"
            android:exported="true">

        </activity>

        <activity android:name=".activities.SeatActivity"
            android:exported="true">


        </activity>

        <activity android:name=".activities.FoodDrinksActivity"
            android:exported="false">

        </activity>

        <activity android:name=".activities.CheckoutActivity"
            android:exported="true">

        </activity>

        <activity android:name=".activities.PaymentActivity"
            android:exported="true">


        </activity>
        
        <activity android:name=".activities.TicketConfirmationActivity"
            android:exported="true">

        </activity>

    </application>

</manifest>